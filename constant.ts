const BASE_URL = "" // The URL of provider's API
const FETCH_INTERVAL = 1000 // The interval in milliseconds of which the application will fetch the data from the provider
const VMIX_SERVER = "http://localhost:8088/api/"

export { BASE_URL, FETCH_INTERVAL, VMIX_SERVER }